@model Project.Models.BookingDto

@{
    ViewData["Title"] = "Edit";
}
<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&display=swap" rel="stylesheet">
<link href="~/css/BookingData.css" rel="stylesheet" />



<hr />
<h1>⨠⨠⨠ UPDATE BOOKING INTERFACE ⨠⨠⨠</h1>

<div class="row">
    <div class="col-md-6">
        <form asp-action="Update" asp-route-id = "@Model.Bid">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Bid" />
            <div class="form-group">
                <label asp-for = "CustomerId"> CUSTOMER IDENTIFIER</label>
                <input asp-for="CustomerId" class="form-control" placeholder="ENTER CUSTOMER ID CODE" />
                <span asp-validation-for="CustomerId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for = "BookingDate"> TEMPORAL COORDINATES</label>
                <input asp-for="BookingDate" class="form-control" type="datetime-local" />
                <span asp-validation-for="BookingDate" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="PaymentMethod"> TRANSACTION PROTOCOL</label>
                <select asp-for="PaymentMethod" class="form-control">
                    <option value="CRYPTO">CRYPTOCURRENCY</option>
                    <option value="CREDIT CARD">CREDIT CARD</option>
                    <option value="DEBIT CARD">DEBIT CARD</option>
                    <option value="UPI">UPI</option>
                    <option value="PAYPAL">PAYPAL</option>
                    <option value="CASH">CASH</option>

                </select>
                <span asp-validation-for="PaymentMethod" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Status"> CONNECTION STATUS</label>
                <select asp-for="Status" class="form-control">
                    <option value="ACTIVE">ACTIVE</option>
                    <option value="PENDING">PENDING SYNCHRONIZATION</option>
                    <option value="TERMINATED">TERMINATED</option>
                </select>
                <span asp-validation-for="Status" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="MODIFY BOOKING" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div class="back-link-container">
    <a asp-action="Index" class="cyber-link">
        <i class="las la-undo"></i>
        RETURN TO MAINFRAME
    </a>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
